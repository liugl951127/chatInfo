server:
  port: 9007

spring:
  application:
    name: cs-customer-success
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/online_chat?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 951127
  data:
    redis:
      host: localhost
      port: 6379
      password:
      timeout: 3000
  jackson:
    time-zone: Asia/Shanghai

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

chat:
  jwt:
    secret: 5vQeSUDLFEt81MPAmQetxV7i8fBJLTSjPCf15seKgOI7PTXou5vgcc4M01aoseUJ
    ttl-ms: 86400000
  m3:
    adapter-url: http://localhost:8084
    default-model: minimax3
  im:
    # cs-im 服务基础 URL (用于 AgentStatsService 调 /api/im/stats/agent/{id})
    # 部署到 k8s 后改为 http://cs-im:9002
    base-url: http://localhost:9002

logging:
  level:
    com.chat: DEBUG

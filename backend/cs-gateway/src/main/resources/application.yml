server:
  port: 9000

spring:
  application:
    name: cs-gateway
  data:
    redis:
      host: localhost
      port: 6379
      password:
      timeout: 3000
  cloud:
    gateway:
      # 关闭按服务名自动路由(单机演示用不到 nacos)
      discovery:
        locator:
          enabled: false
      routes:
        - id: cs-auth
          uri: http://localhost:9001
          predicates:
            - Path=/auth/**
        - id: cs-im
          uri: http://localhost:9002
          predicates:
            - Path=/api/im/**
        # 允许 WebSocket 握手
        - id: cs-im-ws
          uri: http://localhost:9002
          predicates:
            - Path=/ws/**

chat:
  jwt:
    # 64 字节随机 (HS256 要求 ≥ 32); 生产请改用环境变量 / 配置中心
    secret: 5vQeSUDLFEt81MPAmQetxV7i8fBJLTSjPCf15seKgOI7PTXou5vgcc4M01aoseUJ

logging:
  level:
    com.chat: DEBUG
    org.springframework.cloud.gateway: INFO